# Soyli Sahedova — Portfolio (fully editable)

`index.html` is one self-contained file. Open it in a browser (Chrome/Edge work
best) and you can edit **everything** yourself — no code, no tools.

## The edit tool

Bottom-right there's a **✎ Edit page** button. Click it to turn editing on
(click again — **✓ Done editing** — to turn it off). While editing is on:

- **Any text** — click it and type to rewrite it. Headings, paragraphs,
  labels, stats, the `[bracketed placeholders]` — all editable, and the box
  grows automatically as you type. A **formatting bar** appears at the top of
  the screen: change the **font**, **size** (A− / A+), **bold / italic /
  underline**, **text colour**, and **alignment**. **＋ Text** adds a brand-new
  text block right after whatever you're editing (and you can delete blocks you
  added).
- **Any link** — click it and a small box opens. Paste any URL (website,
  Google Drive, YouTube, a PDF link…) **or** click *Attach file…* to attach a
  file from your computer. This is how you set the CV link, the "Open my full
  story presentation" link, each certificate's **View certificate** button,
  each e-mail's **Open full e-mail** link, and so on.
- **Any image** — hover an image slot and use **Upload** / **Remove**. You can
  also drag an image file straight onto a slot. Upload puts your picture in;
  Remove clears it (so you can "unload" assets too).

Everything you change is **saved in this browser automatically** and comes back
every time you reopen the file.

## Sharing your finished portfolio

The saves live in *your* browser, not inside the file itself. To send a copy
with your text, links and images **baked in**:

1. Click **↓ Download shareable copy** (bottom-right).
2. You get `soyli-portfolio-shareable.html`.

Send *that* file. It opens on any computer with your content in place, as a
clean **read-only** portfolio — no edit buttons. Keep editing `index.html` (your
master) and download a fresh shareable copy whenever you update something.

## What changed in this version

- **Profile photo** is now an upload slot (with your current photo as the start).
- **Every picture is editable** — including the case-study screenshots
  (SalesShortcut analytics, YouTube before/after, workflow images, etc.).
- **Personal story** has a button to link your full presentation.
- **Certificates** are now horizontal cards, each with its own **View
  certificate ↗** button so it's clear which one opens.
- **Post carousels** (SalesShortcut, Toyota, 100haryt) no longer auto-scroll —
  scroll them yourself or use the **‹ ›** arrows.
- Each experience starts with a **company intro** — *About the company*,
  *What I was responsible for*, *The result* — ready to fill in.
- **Meta visibility** block removed from SalesShortcut.
- **E-mail cards** now take a screenshot (Upload) and an editable **Open full
  e-mail** link, so they actually work.
- Removed **“video link coming soon.”**
- Recommendation letter replaced with **Real feedback** — 6 slots for
  screenshots of real messages/feedback.
- Removed **Follow along** / **Community presence**.
- Contact now includes **WhatsApp** (+993 68 21 11 8) and **Telegram**
  (@soyli_s).

## Contact form → your inbox

The contact form delivers messages to **sahedowasoyli8@gmail.com** using
FormSubmit (a free service, no account needed).

**One-time activation:** the very first time someone submits the form on the
live page, FormSubmit sends you an e-mail titled roughly *"Activate your form"* —
open it and click **Activate Form** once. After that, every message arrives in
your inbox automatically (with the sender's name, e-mail and message). If you
never submitted it yet, just fill it in once yourself and click the activation
link. If the page is opened offline as a local file, the form falls back to
opening the visitor's e-mail app addressed to you.

### Notes

- Image slots accept image files (PNG/JPEG/WebP…). For **videos**, upload a
  poster image and use the link tool to point to the video (YouTube, Drive…).
- The page was rebuilt as a normal static HTML file (fonts and images are
  embedded), so it's easy to open, edit and share anywhere.
